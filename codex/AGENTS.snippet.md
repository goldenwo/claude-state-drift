<!-- claude-state-drift: paste into your project's AGENTS.md -->
## Project state

This project tracks its objective and current focus in `.claude/state.json`.
At the start of work and whenever direction is unclear, read that file and treat its
`objective` and `current_focus` as the anchor for what you do next. Update it via the
`update-state` skill (`/update-state`) when a deliverable's status changes.
